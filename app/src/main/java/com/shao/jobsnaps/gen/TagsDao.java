package com.shao.jobsnaps.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.shao.jobsnaps.pojo.Tags;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TAGS".
*/
public class TagsDao extends AbstractDao<Tags, Long> {

    public static final String TABLENAME = "TAGS";

    /**
     * Properties of entity Tags.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property TagId = new Property(0, Long.class, "tagId", true, "_id");
        public final static Property TagName = new Property(1, Long.class, "tagName", false, "TAG_NAME");
        public final static Property TagColor = new Property(2, Long.class, "tagColor", false, "TAG_COLOR");
    }


    public TagsDao(DaoConfig config) {
        super(config);
    }
    
    public TagsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TAGS\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: tagId
                "\"TAG_NAME\" INTEGER," + // 1: tagName
                "\"TAG_COLOR\" INTEGER);"); // 2: tagColor
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TAGS\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Tags entity) {
        stmt.clearBindings();
 
        Long tagId = entity.getTagId();
        if (tagId != null) {
            stmt.bindLong(1, tagId);
        }
 
        Long tagName = entity.getTagName();
        if (tagName != null) {
            stmt.bindLong(2, tagName);
        }
 
        Long tagColor = entity.getTagColor();
        if (tagColor != null) {
            stmt.bindLong(3, tagColor);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Tags entity) {
        stmt.clearBindings();
 
        Long tagId = entity.getTagId();
        if (tagId != null) {
            stmt.bindLong(1, tagId);
        }
 
        Long tagName = entity.getTagName();
        if (tagName != null) {
            stmt.bindLong(2, tagName);
        }
 
        Long tagColor = entity.getTagColor();
        if (tagColor != null) {
            stmt.bindLong(3, tagColor);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Tags readEntity(Cursor cursor, int offset) {
        Tags entity = new Tags( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // tagId
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // tagName
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2) // tagColor
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Tags entity, int offset) {
        entity.setTagId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setTagName(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setTagColor(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Tags entity, long rowId) {
        entity.setTagId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Tags entity) {
        if(entity != null) {
            return entity.getTagId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Tags entity) {
        return entity.getTagId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
